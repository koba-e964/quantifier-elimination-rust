[package]
name = "quantifier-elimination"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Exact symbolic quantifier elimination over the real numbers"

[dependencies]
num-bigint = "0.4"
num-integer = "0.1"
num-rational = "0.4"
num-traits = "0.2"

[dev-dependencies]
pretty_assertions = "1"
