name: Snapshot tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  snapshot-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: cargo test --test snapshots
